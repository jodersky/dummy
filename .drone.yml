build:
  unit_tests:
    image: jodersky/ci
    commands:
      - sbt +test

  publish:
    image: jodersky/ci
    when:
      event: push
      success: true
    environment:
      SECURE: "$$SECURE"
      GPG_SSB_ENC: "$$GPG_SSB_ENC"
      BINTRAY_KEY: "$$BINTRAY_KEY"
    commands:
      - .drone/authenv.sh
      - sbt +publishSigned
