build:
  image: jodersky/ci:0.1
  unit_tests:
    commands:
      - sbt +test

  publish:
    when:
      event: push
      success: true
    environment:
      GPG_SSB: "$$GPG_SSB"
      BINTRAY_KEY: "$$BINTRAY_KEY"
    commands:
      - .drone/authenv.sh
      - sbt +publishSigned
