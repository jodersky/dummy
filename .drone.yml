build:
  image: jodersky/ci:0.2
  unit_tests:
    commands:
      - sbt +test

  publish:
    when:
      event: push
      success: true
    environment:
      GPG_SSB_ENC: "$$GPG_SSB_ENC"
      BINTRAY_KEY: "$$BINTRAY_KEY"
    commands:
      - .drone/authenv.sh
      - sbt +publishSigned
